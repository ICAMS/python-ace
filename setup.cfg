# Minimal setup.cfg for compatibility
# Most configuration moved to pyproject.toml

[versioneer]
VCS = git
style = pep440
versionfile_source = src/pyace/_version.py
versionfile_build = pyace/_version.py
tag_prefix =
parentdir_prefix =

[metadata]
license_files = LICENSE.md

[options]
zip_safe = False
